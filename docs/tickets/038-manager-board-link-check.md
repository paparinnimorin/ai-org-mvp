# Ticket 038 — Manager Board Link Check

## Goal
manager board上のタスク参照切れを防ぐため、リンク検証を自動化する。

## Deliverables
- manager board link check CLI
- Makefile target

## Todo
- [x] `scripts/check_manager_board.py` を実装
- [x] `Makefile` に `manager-board-check` target を追加
- [x] `docs/automation/README.md` に運用手順を追記
- [x] `docs/README.md` Scriptsセクションにリンク追加
